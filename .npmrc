# Ensure devDependencies are installed during the build phase.
# Vercel (and Docker with NODE_ENV=production) skips devDeps by default;
# this overrides that so tsc and other build-time tools are available.
node-linker=hoisted
prod=false
