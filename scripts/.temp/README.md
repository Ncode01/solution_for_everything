# Archived / one-off scripts

Scripts here are **not** part of the standard dev or production workflow.

| Script | Purpose |
|--------|---------|
| [push-phase8-schema.ts](./push-phase8-schema.ts) | One-time Neon schema patch (Phase 8). Superseded by `pnpm db:push`. |

**Keep using:** `scripts/diagnose.ts`, `scripts/diagnose-prod.ts`, `scripts/link-owner.ts`, `scripts/auth-seed.ts`.
